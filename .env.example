# Copie para .env e preencha (cp .env.example .env).
# Valores vazios/ausentes usam os defaults, que já funcionam sem configuração.

OPENAI_API_KEY=temp-placeholder
JWT_SECRET=my-secret-key
CLOUDFLARE_TUNNEL_TOKEN=